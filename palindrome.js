var isPalindrome = function(x) {
    //get the input
    //put the input into an array
    //compare first number of the interger to the last number
    //if they are the same numebr , got the next number from both ends
    //keep repeating this prcocess until middle number is read
};